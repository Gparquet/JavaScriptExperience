var user = { firstName: 'Phil', lastName: 'Jean'};
if(user.firstName === "Phil" && user.lastName === "Jeano"){
    console.log(user.firstName);
}else{
    console.log('Nom réalisé');
};

var philFirstName = user.firstName === 'Phil' ? true: false;